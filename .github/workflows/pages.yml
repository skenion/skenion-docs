name: GitHub Pages

on:
  workflow_dispatch:
    inputs:
      train_version:
        description: Lockstep train version to promote, for example 0.43.0.
        required: true
        type: string
      manifest:
        description: Inline release train JSON manifest, or a manifest path in this repository.
        required: true
        type: string
      manifest_ref:
        description: Optional repository ref used when manifest is a path.
        required: false
        type: string
  repository_dispatch:
    types:
      - skenion-train-manual-promote

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  resolve-promotion:
    name: Resolve promotion inputs
    runs-on: ubuntu-latest
    outputs:
      train-version: ${{ steps.resolve.outputs['train-version'] }}
      train-id: ${{ steps.resolve.outputs['train-id'] }}
      manifest: ${{ steps.resolve.outputs.manifest }}
      manifest-ref: ${{ steps.resolve.outputs['manifest-ref'] }}
    steps:
      - uses: actions/checkout@v6
      - id: resolve
        run: node scripts/resolve-manual-promotion-inputs.mjs

  validate-train:
    name: Validate train manifest
    needs: resolve-promotion
    uses: echovisionlab/skenion-ci/.github/workflows/validate-train-manifest.yml@v1
    with:
      manifest: ${{ needs.resolve-promotion.outputs.manifest }}
      manifest-ref: ${{ needs.resolve-promotion.outputs['manifest-ref'] }}
      train-version: ${{ needs.resolve-promotion.outputs['train-version'] }}
      mode: verify

  build:
    name: Build Manual artifact
    runs-on: ubuntu-latest
    needs:
      - resolve-promotion
      - validate-train
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
      SKENION_MANUAL_TRAIN_VERSION: ${{ needs.validate-train.outputs['train-version'] }}
      SKENION_MANUAL_TRAIN_ID: ${{ needs.validate-train.outputs['train-id'] }}
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 10.12.4
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - uses: actions/configure-pages@v6
      - run: pnpm install --frozen-lockfile
      - run: pnpm run docs:check
      - id: promotion
        run: >-
          node scripts/verify-manual-promotion.mjs
          --out .skenion-manual-promotion/manual-promotion.json
      - run: pnpm run build
      - run: |
          mkdir -p build/release
          cp .skenion-manual-promotion/manual-promotion.json build/release/manual-promotion.json
      - uses: actions/upload-pages-artifact@v5
        with:
          path: build

  deploy:
    name: Deploy Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
